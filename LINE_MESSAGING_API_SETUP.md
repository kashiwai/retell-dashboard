# LINE Messaging API 設定ガイド

## LINE Messaging APIとは
LINE Messaging APIは、LINEボットを通じて双方向のコミュニケーションを実現する高度な機能です。
Retellの通話情報を自動的にLINEに送信し、LINEから返信することも可能になります。

## 提供された認証情報
```
Channel ID: 2009276449
Channel Secret: 806e26f795ca18457c4c8803d536ee11
```

## 主な機能

### 1. 自動通知機能
- 通話終了時に自動的にLINEボットからメッセージを送信
- 通話の要約、緊急度、感情分析結果を含む詳細レポート
- カスタマイズ可能なメッセージフォーマット

### 2. 双方向コミュニケーション
- LINEからボットへの問い合わせ対応
- 通話履歴の検索機能
- 特定の通話詳細の取得

### 3. リッチメッセージ機能
- Flex Messageによる視覚的に優れたレポート表示
- ボタンやカードを使った対話的な操作
- 画像、動画、音声ファイルの送信対応

## セットアップ手順

### 1. 環境変数の設定

`.env.local`ファイルに以下を追加：

```bash
# LINE Messaging API
LINE_CHANNEL_ID=2009276449
LINE_CHANNEL_SECRET=806e26f795ca18457c4c8803d536ee11
LINE_CHANNEL_ACCESS_TOKEN=（後述の手順で取得）

# Webhook URL（Vercelデプロイ後）
NEXT_PUBLIC_LINE_WEBHOOK_URL=https://your-app.vercel.app/api/line/webhook
```

### 2. アクセストークンの取得

1. **LINE Developersコンソールにアクセス**
   - https://developers.line.biz/console/
   - LINEビジネスアカウントでログイン

2. **チャンネル設定**
   - Channel ID `2009276449` のチャンネルを選択
   - 「Messaging API」タブを開く

3. **チャンネルアクセストークンの発行**
   - 「チャンネルアクセストークン」セクション
   - 「発行」ボタンをクリック
   - 生成されたトークンをコピー

### 3. Webhook URLの設定

LINE Developersコンソールで：

1. **Webhook設定**
   - Webhook URL: `https://your-app.vercel.app/api/line/webhook`
   - 「Webhookの利用」をONに設定
   - 「検証」ボタンで接続確認

2. **応答設定**
   - 「応答メッセージ」をOFFに設定
   - 「Webhook」をONに設定

### 4. LINE公式アカウントの設定

1. **LINE Official Account Manager**
   - https://manager.line.biz/
   - 該当アカウントを選択

2. **応答設定**
   - 設定 → 応答設定
   - 応答モード：「Bot」を選択
   - あいさつメッセージ：設定

## 実装される機能

### 通話終了時の自動通知

```
━━━━━━━━━━━━━━━━━━
📞 新着通話レポート
━━━━━━━━━━━━━━━━━━

【基本情報】
📱 発信者: 090-1234-5678
⏱ 通話時間: 5分32秒
📅 日時: 2024/03/01 14:30

【要約】
製品の不具合についての問い合わせ。
電源の再起動で解決しました。

【分析結果】
👤 お客様: 田中様
📋 要件: 製品トラブル
⚡ 緊急度: 🟡 中
😊 感情: ポジティブ

【対応状況】
✅ 問題解決済み
📝 フォローアップ不要

━━━━━━━━━━━━━━━━━━
詳細を確認 ▶️
```

### LINEからの問い合わせ対応

ユーザーがLINEで以下のようなメッセージを送信すると、自動応答：

- 「最新の通話」→ 直近5件の通話リストを表示
- 「緊急の通話」→ 緊急度「高」の通話を表示
- 「今日の通話」→ 本日の全通話を表示
- 「統計」→ 本日の通話統計を表示

### リッチメニュー機能

LINEアプリの下部に常時表示されるメニューから：
- 📊 ダッシュボード
- 📞 最新の通話
- 📈 統計情報
- ⚙️ 設定

## API実装詳細

### Webhook受信エンドポイント
`/api/line/webhook` - LINEからのイベントを受信

### メッセージ送信エンドポイント
`/api/line/send` - LINEへのメッセージ送信

### ユーザー情報取得
`/api/line/profile` - LINE ユーザープロフィール取得

## セキュリティ設定

1. **署名検証**
   - Channel Secretを使用してリクエストの署名を検証
   - 不正なリクエストを自動的にブロック

2. **アクセストークン管理**
   - 環境変数で安全に管理
   - 定期的な更新を推奨

3. **レート制限**
   - API呼び出しの制限を実装
   - DDoS攻撃の防御

## トラブルシューティング

### Webhookが受信できない場合

1. **URL確認**
   - HTTPSであることを確認
   - ドメインが正しいか確認

2. **署名検証エラー**
   - Channel Secretが正しいか確認
   - タイムスタンプの同期を確認

3. **ログ確認**
   - Vercel Functions ログを確認
   - LINE Developersコンソールのエラーログを確認

### メッセージが送信できない場合

1. **アクセストークン確認**
   - トークンが有効期限内か確認
   - 権限が正しく設定されているか確認

2. **ユーザーID確認**
   - 送信先ユーザーがボットを友だち追加しているか確認

## 今後の拡張機能

- 音声メッセージの文字起こし
- 通話録音の共有
- AIチャットボット機能
- グループチャットでの共有
- 定期レポートの自動送信
- カスタムアラート設定

## サポート

設定についてご不明な点がございましたら、お気軽にお問い合わせください。