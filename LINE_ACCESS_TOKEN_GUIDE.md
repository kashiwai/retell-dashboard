# LINE Channel Access Token 取得ガイド

## アクセストークンの取得手順

### 1. LINE Developersコンソールにログイン

1. **アクセス**
   - URL: https://developers.line.biz/console/
   - LINEビジネスアカウントでログイン

### 2. チャンネル選択

1. **プロバイダー選択**
   - ログイン後、プロバイダーリストから該当のプロバイダーを選択
   
2. **チャンネル選択**
   - Channel ID: `2009276449` のチャンネルをクリック
   - チャンネル名が表示されているはずです

### 3. Messaging API設定

1. **Messaging APIタブ**
   - 上部のタブから「Messaging API」を選択

2. **チャンネルアクセストークン（長期）**
   - ページをスクロールして「チャンネルアクセストークン（長期）」セクションを探す
   - このセクションは通常、ページの中央付近にあります

### 4. トークンの発行

1. **新規発行の場合**
   - 「発行」ボタンをクリック
   - トークンが生成されて表示されます
   - 例: `1234567890abcdefghijklmnopqrstuvwxyz...`（非常に長い文字列）

2. **既に発行済みの場合**
   - トークンが表示されているはずです
   - 「再発行」ボタンで新しいトークンを生成することも可能

### 5. トークンをコピー

⚠️ **重要**: トークンは一度しか表示されない場合があるので、必ずコピーして安全な場所に保存してください。

### 6. 環境変数に設定

`.env.local`ファイルに追加：

```bash
LINE_CHANNEL_ACCESS_TOKEN=ここにコピーしたトークンを貼り付け
```

## トークンが見つからない場合

### プロバイダーが見つからない場合

1. **新規プロバイダー作成が必要かも**
   - 「プロバイダーを作成」から新規作成
   - Channel ID `2009276449` でチャンネルを検索

### Messaging APIが有効になっていない場合

1. **チャンネル基本設定**で確認
   - チャンネルの種類が「Messaging API」になっているか確認
   - 違う場合は、Messaging APIチャンネルの作成が必要

### 権限がない場合

1. **管理者権限の確認**
   - チャンネルの管理者権限があるか確認
   - 権限がない場合は、管理者に依頼

## トークンの種類

### チャンネルアクセストークン（長期）
- **推奨**: 期限なしで使い続けられる
- 本番環境で使用

### チャンネルアクセストークン（短期）
- 30日で期限切れ
- テスト用途

## セキュリティ注意事項

1. **トークンの管理**
   - Gitにコミットしない
   - 環境変数で管理
   - 定期的に更新を推奨

2. **漏洩時の対応**
   - 即座に「再発行」でトークンを無効化
   - 新しいトークンを環境変数に設定

## 確認方法

トークンが正しく設定されているか確認：

```bash
# ローカルでテスト
curl -X GET https://api.line.me/v2/bot/info \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

成功すると、ボット情報が返ってきます：

```json
{
  "userId": "U1234567890abcdef",
  "basicId": "@123abcde",
  "displayName": "TSUNAGARU.AI Bot",
  "pictureUrl": "https://..."
}
```

## よくある質問

### Q: トークンはどこに表示されますか？

A: LINE Developersコンソール → チャンネル選択 → Messaging API → チャンネルアクセストークン（長期）

### Q: Channel IDとChannel Secretはどこ？

A: 
- **Channel ID**: チャンネル基本設定の上部に表示（`2009276449`）
- **Channel Secret**: チャンネル基本設定の「チャンネルシークレット」欄（`806e26f795ca18457c4c8803d536ee11`）

### Q: トークンを忘れました

A: 再発行が必要です。古いトークンは無効になるので、環境変数の更新を忘れずに。

### Q: 「Invalid channel token」エラー

A: 
1. トークンが正しくコピーされているか確認
2. 前後に空白がないか確認
3. 環境変数が正しく読み込まれているか確認

## サポート

不明な点がございましたら、以下の情報と共にお問い合わせください：
- Channel ID: `2009276449`
- エラーメッセージ
- 実行した手順